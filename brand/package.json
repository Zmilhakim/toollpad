{
  "name": "toollpad-brand",
  "private": true,
  "type": "module",
  "description": "Generates the Toollpad logo, avatar, banner, OG image and launch cards, and the art for each token launched through it",
  "scripts": {
    "render": "node render.mjs",
    "render:token": "node token.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1",
    "sharp": "^0.34.5"
  }
}
